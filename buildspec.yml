---
version: 0.2

phases:
    install:
        runtime-versions:
            nodejs: 12
        commands:
            - npm i
    pre_build:
        commands:
            - echo 'Running linter'
            - npm run lint
    build:
        commands:
            - echo 'Building'
            - npm run generate
artifacts:
    files:
        - ./**/*
    base-directory: '.output/public'
